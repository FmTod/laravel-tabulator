<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up()
    {
        Schema::create('tabulator_persistence', function (Blueprint $table) {
            $table->id();
            // Nullable even when per_user is off: a row with no user is
            // the shared fallback a per-user row overrides. No constraint — the users table is
            // not necessarily on this connection.
            $table->unsignedBigInteger('user_id')->nullable();
            $table->string('table');
            $table->string('type');
            $table->json('data');
            $table->timestamps();
        });
    }

    public function down()
    {
        Schema::dropIfExists('tabulator_persistence');
    }
};
